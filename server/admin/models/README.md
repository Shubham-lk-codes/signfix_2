# Admin data model

Admin-managed records, including allowed cities and WhatsApp logs, use the canonical definitions in `database/schema.sql`. Admin controllers use the shared database adapter so there is one source of truth for every table.
