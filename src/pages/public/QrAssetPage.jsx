import React,{useEffect,useState}from'react';import{CheckCircle2,QrCode,ShieldCheck,Wrench}from'lucide-react';import{get,post}from'../../api/client';
export default function QrAssetPage({token}){const[asset,setAsset]=useState(null),[error,setError]=useState(''),[requesting,setRequesting]=useState(false),[result,setResult]=useState(null);useEffect(()=>{get(`/api/qr/${token}`).then(setAsset).catch(e=>setError(e.message))},[token]);async function submit(e){e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget));try{setResult(await post(`/api/qr/${token}/service`,{...f,photos:[]}));setRequesting(false)}catch(err){setError(err.message)}}return <main className="qr-public"><header><QrCode/><b>SignFix Asset Care</b><span><ShieldCheck size={16}/>Privacy protected</span></header><section className="qr-public-card">{error?<div className="qr-error"><h1>Unable to open asset</h1><p>{error}</p></div>:!asset?<p>Identifying sign board…</p>:<><div className="qr-confirm"><CheckCircle2/><div><small>VERIFIED SIGN BOARD</small><h1>{asset.assetId}</h1></div></div><div className="qr-asset-grid"><p><small>Sign type</small><b>{asset.signType||'—'}</b></p><p><small>Size</small><b>{asset.size||'—'}</b></p><p><small>Material</small><b>{asset.material||'—'}</b></p><p><small>Installed</small><b>{String(asset.installationDate||'—').slice(0,10)}</b></p><p><small>Warranty</small><b>{asset.warrantyActive?'Active':asset.warrantyUntil?`Until ${String(asset.warrantyUntil).slice(0,10)}`:'Not recorded'}</b></p></div><div className="privacy-copy"><ShieldCheck/><p>Customer identity, contact details, exact stored location, photos, and service history are never shown through this public QR page.</p></div>{result?<div className="qr-success"><CheckCircle2/><h2>Request received</h2><p>{result.message}</p><b>{result.ticketId}</b></div>:requesting?<form className="qr-service-form" onSubmit={submit}><h2>Request service</h2><label>Your name<input name="contactName" required minLength="2"/></label><label>Mobile number<input name="contactMobile" required minLength="8" inputMode="tel"/></label><label>Issue type<select name="category"><option>LED Problem</option><option>Electrical Issue</option><option>Physical Damage</option><option>Sign Board Repair</option><option>Replacement</option><option>Cleaning</option><option>Maintenance</option><option>Inspection</option><option>Other</option></select></label><label>Describe the issue<textarea name="issue" rows="4" required minLength="5"/></label><label>Priority<select name="priority"><option value="normal">Normal</option><option value="high">High</option></select></label><button className="primary"><Wrench size={17}/>Submit service request</button><button type="button" className="outline" onClick={()=>setRequesting(false)}>Cancel</button></form>:<button className="primary qr-request" onClick={()=>setRequesting(true)}><Wrench size={18}/>Request service for this sign board</button>}</>}</section><footer>Only the asset’s random QR token is used for identification. Requests are rate-limited and linked internally.</footer></main>}
