import React, { useState } from 'react';
import { useAuth } from '../../context/AuthContext';
import signFixLogo from '../../../branding/signfix-logo.svg';
export default function LoginPage({ navigate }) {const {login}=useAuth();const [email,setEmail]=useState('admin@signfix.in'),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(event){event.preventDefault();setBusy(true);setError('');try{await login({email:email.trim(),password,portal:'admin'});navigate('/')}catch(requestError){setError(requestError.status===401?'Invalid email or password.':requestError.message)}finally{setBusy(false)}}return <div className="auth-page"><form className="auth-card" onSubmit={submit}><img src={signFixLogo} alt="SignFix"/><p className="auth-eyebrow">ADMIN OPERATIONS</p><h1>Welcome to SignFix</h1><p>Sign board sales, service and maintenance management</p><label>Email<input value={email} onChange={event=>setEmail(event.target.value)} name="email" type="email" required autoComplete="username"/></label><label>Password<input value={password} onChange={event=>setPassword(event.target.value)} name="password" type="password" required autoComplete="current-password"/></label><button className="demo-login" type="button" onClick={()=>{setEmail('admin@signfix.in');setPassword('SignFix@123')}}>Use admin demo credentials</button>{error&&<div className="auth-error">{error}</div>}
<button className="primary" disabled={busy}>{busy?'Signing in…':'Sign in to admin panel'}</button><small>Authorized administrators only</small></form></div>}
