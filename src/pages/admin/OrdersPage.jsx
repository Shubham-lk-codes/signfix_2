import React,{useEffect,useState}from'react';import{ArrowLeft,Eye,Save}from'lucide-react';import{get,patch}from'../../api/client';import StatusBadge from'../../components/ui/StatusBadge';
const statuses=['new','under_review','quotation','approved','production','ready','installation','completed','cancelled'];
export default function OrdersPage(){const[rows,setRows]=useState([]),[filter,setFilter]=useState(''),[search,setSearch]=useState(''),[order,setOrder]=useState(null),[error,setError]=useState('');async function load(){try{setRows((await get(`/api/orders?status=${filter}&search=${encodeURIComponent(search)}`)).data||[])}catch(e){setError(e.message)}}useEffect(()=>{load()},[filter]);async function open(id){setOrder(await get(`/api/orders/${id}`))}async function save(e){e.preventDefault();const f=new FormData(e.currentTarget),specifications={product:f.get('product'),material:f.get('material'),lighting:f.get('lighting'),length:Number(f.get('length')),width:Number(f.get('width')),quantity:Number(f.get('quantity'))};const updated=await patch(`/api/orders/${order.id}`,{specifications,adminNotes:f.get('adminNotes'),technicianId:f.get('technicianId')?Number(f.get('technicianId')):null,status:f.get('status')});setOrder(updated);await load()}if(order)return <section className="content"><div className="headline"><div><button className="text-button" onClick={()=>setOrder(null)}><ArrowLeft size={16}/>Orders</button><h1>{order.id}</h1><small>{order.customer} · <StatusBadge>{order.status}</StatusBadge></small></div></div><form className="card order-editor" onSubmit={save}><h2>Order specifications</h2><div className="order-form-grid">{['product','material','lighting','length','width','quantity'].map(k=><label key={k}>{k}<input name={k} type={['length','width','quantity'].includes(k)?'number':'text'} step="any" defaultValue={order[k]||''}/></label>)}<label>Status<select name="status" defaultValue={order.status}>{statuses.map(s=><option key={s}>{s}</option>)}</select></label><label>Installation technician<select name="technicianId" defaultValue={order.technicianId||''}><option value="">Unassigned</option>{order.technicians.map(t=><option value={t.id} key={t.id}>{t.name} · {t.mobile}</option>)}</select></label><label className="wide">Admin notes<textarea name="adminNotes" rows="4" defaultValue={order.adminNotes||''}/></label></div><button className="primary"><Save size={16}/>Save changes</button></form><article className="card profile-section"><h2>Design review</h2>{order.designs.length?order.designs.map(d=><div className="order-related" key={d.id}><b>Design request #{d.id}</b><StatusBadge>{d.status}</StatusBadge><pre>{JSON.stringify(d.requirements,null,2)}</pre></div>):<p className="empty-copy">No design submitted.</p>}</article><article className="card profile-section"><h2>Quotations</h2>{order.quotations.map(q=><div className="order-related" key={q.quotationNo}><b>{q.quotationNo}</b><span>₹{Number(q.finalAmount).toLocaleString('en-IN')}</span><StatusBadge>{q.status}</StatusBadge></div>)}</article></section>;return <section className="content"><div className="headline"><div><p>SALES</p><h1>Order management</h1><small>Review orders from submission through installation and completion.</small></div></div><div className="status-tabs"><button className={!filter?'active':''} onClick={()=>setFilter('')}>All</button>{statuses.map(s=><button className={filter===s?'active':''} onClick={()=>setFilter(s)} key={s}>{s.replaceAll('_',' ')}</button>)}</div><div className="resource-toolbar"><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Order, customer or product"/><button className="outline" onClick={load}>Search</button></div>{error&&<div className="auth-error">{error}</div>}<article className="tablecard card"><table><thead><tr><th>Order ID</th><th>Customer</th><th>Product</th><th>Amount</th><th>Status</th><th>Date</th><th/></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><b>{r.id}</b></td><td>{r.customer}</td><td>{r.product||'—'}</td><td>₹{Number(r.estimatedPrice||0).toLocaleString('en-IN')}</td><td><StatusBadge>{r.status}</StatusBadge></td><td>{new Date(r.createdAt).toLocaleDateString()}</td><td><button className="outline" onClick={()=>open(r.id)}><Eye size={15}/>View</button></td></tr>)}</tbody></table></article></section>}
