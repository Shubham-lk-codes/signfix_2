import React, { useState } from 'react';
import { get } from '../../api/client';
export default function ReportsPage(){const [report,setReport]=useState(null),[error,setError]=useState('');async function generate(type){try{setReport(await get(`/api/reports/${type}`))}catch(e){setError(e.message)}}return <section className="content"><div className="headline"><div><p>ANALYTICS</p><h1>Reports</h1><small>Generate current operational summaries.</small></div></div><div className="report-grid">{['sales','orders','services','technicians','customers','revenue','ai-leads'].map(type=><button className="card" key={type} onClick={()=>generate(type)}>{type.replace('-',' ')}</button>)}</div>{error&&<div className="auth-error">{error}</div>}{report&&<pre className="card report-output">{JSON.stringify(report,null,2)}</pre>}</section>}
