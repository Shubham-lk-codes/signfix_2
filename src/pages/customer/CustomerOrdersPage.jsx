import React, { useEffect, useState } from 'react';import { get } from '../../api/client';import DataTable from '../../components/ui/DataTable';import StatusBadge from '../../components/ui/StatusBadge';
export default function CustomerOrdersPage(){const[rows,setRows]=useState([]);useEffect(()=>{get('/api/orders').then(r=>setRows(r.data))},[]);return <><h1>My Orders</h1><DataTable rows={rows} columns={[{key:'id',label:'Order ID'},{key:'product',label:'Product'},{key:'estimatedPrice',label:'Estimated Price',render:v=>`₹${Number(v||0).toLocaleString('en-IN')}`},{key:'status',label:'Status',render:v=><StatusBadge>{v}</StatusBadge>}]}/></>}
