import React, { useState } from 'react';import { post } from '../../api/client';
export default function AiSupportPage(){const[messages,setMessages]=useState([{from:'ai',text:'Hello! I am the SignFix AI Assistant. What kind of sign board do you need?'}]),[text,setText]=useState('');async function send(e){e.preventDefault();if(!text.trim())return;const question=text;setText('');setMessages(x=>[...x,{from:'user',text:question}]);const result=await post('/api/ai/chat',{message:question});setMessages(x=>[...x,{from:'ai',text:result.reply,actions:result.actions}])}return <div className="chat-page"><h1>SignFix AI Assistant</h1><div className="chat-log">{messages.map((m,i)=><div className={`chat ${m.from}`} key={i}>{m.text}{m.actions&&<div>{m.actions.map(a=><button key={a}>{a}</button>)}</div>}</div>)}</div><form className="chat-input" onSubmit={send}><input value={text} onChange={e=>setText(e.target.value)} placeholder="Ask about products, pricing or service…"/><button>Send</button></form><small>Guidance only. Final feasibility, quotations and delivery dates require Admin approval.</small></div>}
