import React from 'react';
import { Home, LogOut, MessageCircle, Package, User, Wrench } from 'lucide-react';
import { useAuth } from '../../context/AuthContext';
import signFixLogo from '../../../branding/signfix-logo.svg';
export default function MobileLayout({ path, navigate, children, technician = false }) { const { user, logout } = useAuth(); const items = technician ? [['Home','/technician',Home],['Jobs','/technician/jobs',Wrench],['Profile','/profile',User]] : [['Home','/customer',Home],['Orders','/customer/orders',Package],['Service','/customer/service',Wrench],['AI Support','/customer/ai',MessageCircle],['Profile','/profile',User]]; return <div className="mobile-app"><header className="mobile-header"><div className="brand"><img className="brandlogo" src={signFixLogo} alt="SignFix logo"/><div><b>SignFix</b><small>{technician ? 'Technician' : 'Customer'} App</small></div></div><button className="logout" onClick={logout}><LogOut/></button></header><div className="mobile-content">{children}</div><nav className="bottom-nav">{items.map(([label,url,Icon])=><button key={url} className={path===url?'active':''} onClick={()=>navigate(url)}><Icon size={20}/><span>{label}</span></button>)}</nav></div>; }
